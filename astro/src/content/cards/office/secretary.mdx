---
title: Colonial Secretary
deck:
   reference: office
background:
   image: "@assets/images/cards/office-sec.jpeg"
card-back:
   reference: office/secretary-back
---
import DiceCheck from "@components/DiceCheck.astro";
import Layout from "@components/OfficeCardLayout.astro";

<Layout {...frontmatter} textSize={70}>
{/* **First**, draw a {frontmatter._["deck.trades.singular"].toLowerCase()} card. */}
{/* **Next**, decide the order of operations.\ */}

Decide the order of operations,\
including Trade in America.

May take Oversight action:

<DiceCheck>
   <Fragment slot="dice-pool">
      <div class="big">3 Dice</div>

      _May add a die per £1 spent._
      _Lower after each check._
   </Fragment>
   <div slot="dice-success" class="secretary-success">
      Add one military unit, **or**\
      open an order in America.

      If the order is in a
      <span style="font-weight: 600">colonized</span>
      region, add an unrest cube\
      to it for each 6 rolled.
   </div>
</DiceCheck>

May take one debt during turn.

</Layout>

{/*
      _May use military units\
      to add dice._
*/}

<style>{`
.hg {
   p::first-line {
      line-height: 1.2em;
   }
}
.secretary-success {
   flex-grow: 1;
   display: grid;

   p {
      line-height: 1.2em;
   }
}
.big {
   font-family: 'Libre Bodoni Variable';
   font-size: 13pt;
   line-height: 1.25em;
   letter-spacing: -0.5px;
   text-align: center;
   margin-bottom: 0.25em;
}
`}</style>
