---
title: Colonial Secretary
deck:
   reference: office
background:
   image: "@assets/images/cards/office-sec.jpeg"
card-back:
   reference: office/secretary-back
---
import DiceCheck from "@components/DiceCheck.astro";
import Layout from "@components/OfficeCardLayout.astro";

<Layout {...frontmatter} textSize={70}>
Decide the order of operations.\
May take Funding action.

May take Oversight action:

<DiceCheck>
   <Fragment slot="dice-pool">
      <div class="secretary-dice">
         4

         __+__ (£)

         dice.
      </div>

      _Lower after each check._
   </Fragment>
   <div slot="dice-success" class="secretary-success">
      Add a military unit to the
      Army of North America\
      **or** open an order in a\
      foreign-settled region.
   </div>
</DiceCheck>
</Layout>

{/*
      _May use military units\
      to add dice._
*/}

<style>{`
.secretary-success {
   flex-grow: 1;

   p {
      line-height: 1.1em;
      font-size: 10px;
      margin: 0.6em 0;
   }
}
.secretary-dice {
   height: 1.75rem;
   display: grid;

   grid-template-areas:
      "a b ."
      "a c c";
   justify-content: center;
   margin-bottom: 0.25em;

   p {
      margin-bottom: 0;
      display: block;
   }
   p:first-child {
      font-family: 'Libre Bodoni Variable';
      font-size: 22pt;
      line-height: 2rem;
      text-align: center;
      grid-area: a;
   }
   p:nth-child(2) {
      grid-area: b;
      align-self: center;
      font-size: 8pt;
      line-height: 7pt;
   }
   p:nth-child(3) {
      grid-area: c;
      font-size: 8pt;
      line-height: 7pt;
      justify-self: end;
   }
}
`}</style>
