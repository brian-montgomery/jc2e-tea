---
title: Quartering Required
regions:
   - NE
   - M
---
import Layout from "@components/ActCardLayout.astro";

<Layout title={frontmatter.title} regions={frontmatter.regions} textSize={90}>
   When calculating expenses,
   the **{frontmatter._["role.govt.governor.full"]}**
   should roll one die for each regiment.

   For each success, ignore the cost of one regiment.
   For each catastrophic failure, add an **unrest cube**.
</Layout>