---
title: Peace
deck:
   reference: events
   quantity: 1
card-back:
   reference: events/backs/chesapeake
---
import Layout from "@components/EventCardLayout.astro";

<Layout title={frontmatter.title} textSize={100}>
Open any orders that are connected through the border
of the region and its circle (●) symbol.

For regions that are **colonized**,\
remove one **unrest cube**. \
For other regions, add one strength.
</Layout>